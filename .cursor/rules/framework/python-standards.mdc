---
description: Padrões específicos de desenvolvimento Python
globs: **/*.py
---

# 🐍 Padrões Python

## Estrutura de Código
- SEMPRE use type hints para funções públicas
- SEMPRE inclua docstrings em classes e funções
- PREFIRA classes a funções quando há estado
- Use snake_case para variáveis e funções
- Use PascalCase para classes

## Imports e Organização
```python
# Ordem: stdlib, third-party, local
import os
import re
from typing import Dict, List

import requests
from bs4 import BeautifulSoup

from src.models import Document
from src.utils import logger
```

## Tratamento de Erros
- SEMPRE trate exceções explicitamente
- SEMPRE registre erros com contexto
- Use logging estruturado:
```python
import logging
logger = logging.getLogger(__name__)

try:
    result = process_document()
    logger.info(f"Document processed: {document_id}")
except Exception as e:
    logger.error(f"Failed to process {document_id}: {str(e)}")
    raise
```

## Configurações
- SEMPRE use variáveis de ambiente
- SEMPRE valide configurações na inicialização
- Use python-dotenv para .env files

## Performance
- Use list comprehensions quando apropriado
- Evite loops aninhados desnecessários
- Use generators para grandes datasets